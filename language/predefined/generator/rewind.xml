<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
<!-- EN-Revision: 5e6944e79f2b19783627d76bd7fd1411feb23c65 Maintainer: 邹松 Status: ready -->
<!-- CREDITS: mowangjuanzi -->
<refentry xml:id="generator.rewind" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 <refnamediv>
  <refname>Generator::rewind</refname>
  <refpurpose>执行生成器，直至并包含第一个 yield 语句</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis role="Generator">
   <modifier>public</modifier> <type>void</type><methodname>Generator::rewind</methodname>
   <void/>
  </methodsynopsis>
  <para>
   执行生成器，直至并包含<emphasis>第一个</emphasis> &yield;。若生成器已位于<emphasis>第一个</emphasis> &yield;
   处，则不执行任何操作。若生成器曾向前推进超过某个 &yield; 表达式，则此方法将抛出 <classname>Exception</classname>。
  </para>
  <note>
   <para>
    这是开始  &foreach; 循环时调用的<emphasis>第一个</emphasis>方法，不会在 &foreach; 循环<emphasis>结束后</emphasis>执行。
   </para>
  </note>
 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  &no.function.parameters;
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>
   &return.void;
  </para>
 </refsect1>

 <refsect1 role="examples">
  &reftitle.examples;
  <para>
   <example>
    <title><methodname>Generator::rewind</methodname> 示例</title>
    <programlisting role="php">
<![CDATA[
<?php

function generator(): Generator
{
    echo "I'm a generator!\n";

    for ($i = 1; $i <= 3; $i++) {
        yield $i;
    }
}

// 初始化生成器
$generator = generator();

// 将生成器重置到第一个 yield 表达式的起始位置（如果尚未处于该位置）
$generator->rewind(); // I'm a generator!

// 此处无任何操作；生成器已处于重置状态
$generator->rewind(); // 没有输出（NULL）

// 会将生成器重置到第一个 yield 表达式（如果尚未处于该位置），并对其进行遍历
foreach ($generator as $value) {
    // 在生成第一个值后，生成器会停留在第一个 yield 表达式处，直到恢复执行并继续前进到下一个 yield
    echo $value, PHP_EOL; // 1

    break;
}

// 恢复执行后再次重置，不会产生错误，因为生成器尚未越过第一个 yield
$generator->rewind();

echo $generator->current(), PHP_EOL; // 1

// 不会产生错误，生成器仍处于第一个 yield 处
$generator->rewind();

// 此操作将生成器推进至第二个 yield 表达式
$generator->next();

try {
    // 此操作会抛出异常，因为生成器已推进至第二个 yield
    $generator->rewind(); // Fatal error: Uncaught Exception: Cannot rewind a generator that was already run
} catch (Exception $e) {
    echo $e->getMessage();
}

?>
]]>
    </programlisting>
    &example.outputs;
    <screen>
<![CDATA[
I'm a generator!
1
1
Cannot rewind a generator that was already run
]]>
    </screen>
   </example>
  </para>
 </refsect1>

</refentry>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
