<?xml version="1.0" encoding="gb2312"?>
<!-- $Revision: 1.1 $ -->
<!-- $Author: dallas $ -->
<!-- EN-Revision: 1.8 Maintainer: dallas Status: ready -->
  <refentry id="function.imagettftext">
   <refnamediv>
    <refname>imagettftext</refname>
    <refpurpose>用 TrueType 字体向图像写入文本</refpurpose>
   </refnamediv>
   <refsect1>
    <title>说明</title>
    <methodsynopsis>
     <type>array</type><methodname>imagettftext</methodname>
     <methodparam><type>resource</type><parameter>image</parameter></methodparam>
     <methodparam><type>int</type><parameter>size</parameter></methodparam>
     <methodparam><type>int</type><parameter>angle</parameter></methodparam>
     <methodparam><type>int</type><parameter>x</parameter></methodparam>
     <methodparam><type>int</type><parameter>y</parameter></methodparam>
     <methodparam><type>int</type><parameter>color</parameter></methodparam>
     <methodparam><type>string</type><parameter>fontfile</parameter></methodparam>
     <methodparam><type>string</type><parameter>text</parameter></methodparam>
    </methodsynopsis>
    <para>
     <function>imagettftext</function> 将字符串
     <parameter>text</parameter> 画到
     <parameter>image</parameter> 所代表的图像上，从坐标
     <parameter>x</parameter>，<parameter>y</parameter>（左上角为
     0, 0）开始，角度为 <parameter>angle</parameter>，颜色为
     <parameter>color</parameter>，使用
     <parameter>fontfile</parameter> 所指定的 TrueType 字体文件。根据
     PHP 所使用的 GD 库的不同，如果 <parameter>fontfile</parameter>
     没有以 '/'开头，则 '.ttf' 将被加到文件名之后并且会搜索库定义字体路径。
    </para>
    <para>
     由 <parameter>x</parameter>，<parameter>y</parameter>
     所表示的坐标定义了第一个字符的基本点（大概是字符的左下角）。这和
     <function>imagestring</function> 不同，其 x，y 定义了第一个字符的右上角。
    </para>
    <para>
     <parameter>angle</parameter> 以角度表示，0 度为从左向右阅读文本（3
     点钟方向），更高的值表示逆时针方向（即如果值为 90 则表示从下向上阅读文本）。
    </para>
    <para>
     <parameter>fontfile</parameter> 是想要使用的 TrueType 字体的文件名。
    </para>
    <para>
     <parameter>text</parameter> 是文本字符串，可以包含
     UTF-8 字符序列（形式为：&amp;#123;）来访问字体中超过前 255 个的字符。
    </para>
    <para>
     <parameter>color</parameter> 是颜色的索引值。使用某颜色索引值的负值具有关闭防混色的效果。
    </para>
    <para>
     <function>imagettftext</function> 返回一个含有 8
     个单元的数组表示了文本外框的四个角，顺序为坐下角，右下角，右上角，左上角。这些点是相对于文本的而和角度无关，因此“左上角”指的是以水平方向看文字时其左上角。
    </para>
    <para>
     本例中的脚本将生成一个黑色的 400x30 的 JPEG 图像，其中有白色
     Arial 字体写的 "Testing..."。
     <example>
      <title><function>imagettftext</function> 例子</title>
      <programlisting role="php">
<![CDATA[
<?php
  header("Content-type: image/jpeg");
  $im = imagecreate(400,30);
  $white = imagecolorallocate($im, 255,255,255);
  $black = imagecolorallocate($im, 0,0,0);

  // Replace path by your own font path
  imagettftext($im, 20, 0, 10, 20, $black, "/path/arial.ttf",
  "Testing... Omega: &amp;#937;");
  imagejpeg($im);
  imagedestroy($im);
?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     本函数同时需要 GD 库和 <ulink
     url="&url.freetype;">FreeType</ulink> 库。
    </para>
    <para>
     参见 <function>imagettfbbox</function>。
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
