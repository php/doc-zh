<?xml version="1.0" encoding="gb2312"?>
<!-- $Revision: 1.6 $ -->
<!-- $Author: dallas $ -->
<!-- EN-Revision: 1.5 Maintainer: cysoft Status: ready -->
 <reference id="ref.w32api">
  <title>W32api 函数库</title>
  <titleabbrev>W32api</titleabbrev>

  <partintro>
   <section id="w32api.intro">
    &reftitle.intro;
    <para>
     这是对于 DLL 的通用扩展 API。它最初是用来允许从 PHP 中访问
     Win32 API，尽管这样你仍然可以通过它访问其它 DLL。
    </para>
    <para>
     这个函数支持当前 PHP 标准的类型（string，boolean，float，integer 和 null）以及你使用
     <function>w32api_deftype</function> 函数所自定义的类型。
    </para>
    &warn.experimental;
   </section>

   <section id="w32api.requirements">
    &reftitle.required;
    <para>
     这个扩展只能工作在 Windows 系统中。
    </para>
   </section>

   <section id="w32api.installation">
    &reftitle.install;
    &no.install;
   </section>

   <section id="w32api.configuration">
    &reftitle.runtime;
    &no.config;
   </section>

   <section id="w32api.resources">
    &reftitle.resources;
    <para>
     这个扩展定义了一个资源类型，被用作用户自定义类型。这个类型的名称是
     <literal>"dynaparm"</literal>。
    </para>
   </section>

   &reference.w32api.constants;

   <section id="w32api.examples">
    &reftitle.examples;
    <para>
     这个例子演示如何得到系统持续运行的时间，并把它显示在一个消息对话框中。
    </para>
    <example>
     <title>得到系统持续运行的时间，并把它显示在消息对话框中</title>
     <programlisting role="php">
<![CDATA[
<?php
// Define constants needed, taken from
// Visual Studio/Tools/Winapi/WIN32API.txt
define("MB_OK", 0);

// Load the extension in
dl("php_w32api.dll");

// Register the GetTickCount function from kernel32.dll
w32api_register_function("kernel32.dll",
                         "GetTickCount",
                         "long");

// Register the MessageBoxA function from User32.dll
w32api_register_function("User32.dll",
                         "MessageBoxA",
                         "long");

// Get uptime information
$ticks = GetTickCount();

// Convert it to a nicely displayable text
$secs  = floor($ticks / 1000);
$mins  = floor($secs / 60);
$hours = floor($mins / 60);

$str = sprintf("You have been using your computer for:".
                "\r\n %d Milliseconds, or \r\n %d Seconds".
                "or \r\n %d mins or\r\n %d hours %d mins.",
                $ticks,
                $secs,
                $mins,
                $hours,
                $mins - ($hours*60));

// Display a message box with only an OK button and the uptime text
MessageBoxA(NULL,
            $str,
            "Uptime Information",
            MB_OK);
?>
]]>
     </programlisting>
    </example>
   </section>
  </partintro>

  &reference.w32api.functions;

 </reference>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

