<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
<!-- EN-Revision: ddcf673c59d1708557680946fbffeee361d7fca6 Maintainer: mowangjuanzi Status: ready -->
<refentry xml:id="yaf-route-interface.route" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 <refnamediv>
  <refname>Yaf_Route_Interface::route</refname>
  <refpurpose>路由请求</refpurpose>
 </refnamediv>

 <refsect1 role="description">
  &reftitle.description;
  <methodsynopsis>
   <modifier>abstract</modifier> <modifier>public</modifier> <type>bool</type><methodname>Yaf_Route_Interface::route</methodname>
   <methodparam><type>Yaf_Request_Abstract</type><parameter>request</parameter></methodparam>
  </methodsynopsis>
  <para>
    <methodname>Yaf_Route_Interface::route</methodname> 是用户自定义路由唯一需要实现的方法。
    <note>
     <simpara>
      自 2.3.0 起，还应该实现另外一个方法，参阅 <methodname>Yaf_Route_Interface::assemble</methodname>。
     </simpara>
    </note>
  </para>
  <para>
    如果此方法返回 &true;，那么路由过程将会中止。否则，
    <classname>Yaf_Router</classname> 将会调用路由栈中的下一个路由来路由请求。
  </para>

  <para>
    此方法通过调用 <methodname>Yaf_Request_Abstract::setControllerName</methodname>、
    <methodname>Yaf_Request_Abstract::setActionName</methodname> 和
    <methodname>Yaf_Request_Abstract::setModuleName</methodname>
    将路由结果设置到参数 request 中。
   </para>

   <para>
    这个方法最后还应该调用
    <methodname>Yaf_Request_Abstract::setRouted</methodname> 将请求标记为已路由。
   </para>

 </refsect1>

 <refsect1 role="parameters">
  &reftitle.parameters;
  <variablelist>
   <varlistentry>
    <term><parameter>request</parameter></term>
    <listitem>
     <para>
       <classname>Yaf_Request_Abstract</classname> 实例。
     </para>
    </listitem>
   </varlistentry>
  </variablelist>
 </refsect1>

 <refsect1 role="returnvalues">
  &reftitle.returnvalues;
  <para>

  </para>
 </refsect1>


 <refsect1 role="examples">
  &reftitle.examples;
  <example>
   <title><methodname>Yaf_Route_Interface::route</methodname> 示例</title>
   <programlisting role="php">
<![CDATA[
<?php
class ApiRoute implements Yaf_Route_Interface {

    private $_prefix;

    public function __construct(string $prefix) {
        $this->_prefix = $prefix;
    }

    public function route(Yaf_Request_Abstract $request): bool {
        $uri = $request->getRequestUri();
        if (0 !== strpos($uri, $this->_prefix)) {
            /* 不归我们管，让路由器尝试下一个路由 */
            return false;
        }

        /* 把 URI "/api/user/list" 解析为 controller/action */
        $segments = explode("/", ltrim(substr($uri, strlen($this->_prefix)), "/"));
        $controller = isset($segments[0]) ? ucfirst($segments[0]) : NULL;
        $action = isset($segments[1]) ? $segments[1] : NULL;

        if (empty($controller)) {
            return false;
        }

        $request->setModuleName("index");
        $request->setControllerName($controller);
        $request->setActionName($action);
        $request->setRouted(); /* 路由过程到此结束 */

        return true;
    }

    public function assemble(array $info, ?array $query = null): string {
        $uri = $this->_prefix . "/" . strtolower($info[':c']);
        if (isset($info[':a'])) {
            $uri .= "/" . $info[':a'];
        }

        if (!empty($query)) {
            $uri .= "?" . http_build_query($query);
        }

        return $uri;
    }
}

/**
 * 注册自定义路由
 */
Yaf_Dispatcher::getInstance()->getRouter()->addRoute("api", new ApiRoute("/api"));
?>
]]>
   </programlisting>
   &example.outputs.similar;
   <screen>
<![CDATA[
/* http://yourdomain.com/api/user/list
 * 会路由为以下值：
 */
array(
  "module"     => "index",
  "controller" => "User",
  "action"     => "list",
)
]]>
   </screen>
  </example>
 </refsect1>

 <refsect1 role="seealso">
  &reftitle.seealso;
  <simplelist>
   <member><methodname>Yaf_Route_Interface::assemble</methodname></member>
   <member><methodname>Yaf_Router::addRoute</methodname></member>
   <member><methodname>Yaf_Request_Abstract::setRouted</methodname></member>
  </simplelist>
 </refsect1>

</refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
