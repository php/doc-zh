<?xml version="1.0" encoding="utf-8"?>
<!-- $Revision$ -->
<!-- EN-Revision: ddcf673c59d1708557680946fbffeee361d7fca6 Maintainer: mowangjuanzi Status: ready -->
<chapter xml:id="yaf.tutorials" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink">
 &reftitle.examples;

 <section xml:id="yaf.tutorials.quickstart">
  <title>快速入门</title>

 <example>
  <title>一个典型的应用目录结构</title>
  <screen>
<![CDATA[
- .htaccess           // 重写规则
+ public/
  | - index.php       // 应用入口
  | + css/
  | + js/
  | + img/
+ conf/
  | - application.ini // 应用配置
- application/
  - Bootstrap.php     // 引导
  + controllers/
     - Index.php      // 默认控制器
  + views/
     |+ index/
        - index.phtml // 默认动作的视图模板
  + library/          // 类库
  + models/           // 模型
  + plugins/          // 插件
]]>
  </screen>
  <simpara>
   将 web 服务器的 <literal>DocumentRoot</literal> 设置为
   <filename>public</filename> 目录，这样从 web 就只能访问公开资源。
  </simpara>
 </example>

 <example>
  <title>多模块应用的目录结构</title>
  <screen>
<![CDATA[
+ public/
+ conf/
+ application/
  + modules/
    + Index/       // 默认模块
      + controllers/
      + views/
    + Admin/       // 另一个模块
      + controllers/
      + views/
  + library/
  + models/
  + plugins/
  - Bootstrap.php
]]>
  </screen>
  <simpara>
   在多模块应用中，每个模块在
   <literal>application/modules/&lt;ModuleName&gt;/</literal> 下都有自己的
   <literal>controllers</literal> 和 <literal>views</literal> 目录。
   已注册的模块在
   <link linkend="configuration.yaf.modules">application.modules</link> 中声明。
  </simpara>
 </example>

 <example>
  <title>入口</title>
  <simpara>
   public 目录下的 <filename>index.php</filename> 是整个应用的唯一入口，
   应该把所有请求都重写到它（在 Apache + mod_php 下可以使用
   <filename>.htaccess</filename>，或者你的 web 服务器中的等效配置，见下文）。
  </simpara>
   <programlisting role="php">
<![CDATA[
<?php
define("APPLICATION_PATH",  dirname(dirname(__FILE__)));

$app  = new Yaf_Application(APPLICATION_PATH . "/conf/application.ini");
$app->bootstrap() //call bootstrap methods defined in Bootstrap.php
 ->run();
?>
]]>
   </programlisting>
 </example>

 <example>
  <title>重写规则</title>
  <screen>
<![CDATA[
#for apache (.htaccess)
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule .* index.php

#for nginx
server {
  listen 80;
  server_name domain.com;
  root   document_root;
  index  index.php index.html index.htm;

  location / {
    try_files $uri $uri/ /index.php?$args;
  }
}

#for lighttpd
$HTTP["host"] =~ "(www.)?domain.com$" {
  url.rewrite = (
     "^/(.+)/?$"  => "/index.php/$1",
  )
}
]]>
  </screen>
 </example>

 <example>
  <title>引导</title>
  <simpara>
   <classname>Bootstrap</classname> 类中所有名称以 <literal>_init</literal>
   开头的方法都会被 <methodname>Yaf_Application::bootstrap</methodname>
   按定义顺序自动调用。每个这样的方法都会收到
   <classname>Yaf_Dispatcher</classname> 实例作为参数。
   名称为其他名称的方法不会被自动调用。
  </simpara>
  <programlisting role="php">
<![CDATA[
<?php
class Bootstrap extends Yaf_Bootstrap_Abstract
{
    public function _initConfig(Yaf_Dispatcher $dispatcher)
    {
        // called first
    }

    public function _initPlugin(Yaf_Dispatcher $dispatcher)
    {
        // called second
    }

    public function _initRoute(Yaf_Dispatcher $dispatcher)
    {
        // called third
    }
}
?>
]]>
  </programlisting>
 </example>

 <example>
  <title>应用配置</title>
  <simpara>
   <filename>application.ini</filename> 是应用的配置文件。
   其中可以使用 <filename>index.php</filename> 中定义的常量，
   并且各个分节之间可以相互继承。
  </simpara>
   <programlisting role="ini">
<![CDATA[
[yaf]
;APPLICATION_PATH is the constant defined in index.php
application.directory=APPLICATION_PATH "/application/"

;product section inherit from yaf section
[product:yaf]
foo=bar
]]>
   </programlisting>
 </example>

 <example>
  <title>使用 PHP 数组作为应用配置</title>
  <simpara>
   除了 INI 配置文件，也可以把一个普通的 PHP 数组作为配置传递给
   <methodname>Yaf_Application::__construct</methodname>。
  </simpara>
   <programlisting role="php">
<![CDATA[
<?php
$config = array(
    "application" => array(
        "directory" => APPLICATION_PATH . "/application/",
    ),
);

$app = new Yaf_Application($config);
?>
]]>
   </programlisting>
 </example>

 <example>
  <title>默认控制器</title>
  <simpara>
   在 Yaf 中，默认控制器名为 <classname>IndexController</classname>：
  </simpara>
   <programlisting role="php">
<![CDATA[
<?php
class IndexController extends Yaf_Controller_Abstract
{
    // default action name
    public function indexAction()
    {
        $this->getView()->content = "Hello World";
    }
}
?>
]]>
   </programlisting>
 </example>

 <example>
  <title>默认视图模板</title>
  <simpara>
   默认控制器/动作的视图脚本是
   <filename>application/views/index/index.phtml</filename>。Yaf 提供了一个
   名为 <classname>Yaf_View_Simple</classname> 的小型视图引擎，
   它的模板是普通的 PHP 脚本。
  </simpara>
   <programlisting role="php">
<![CDATA[
<html>
 <head>
   <title>Hello World</title>
 </head>
 <body>
   <?php echo $content;?>
 </body>
</html>
]]>
   </programlisting>
 </example>

 <example>
  <title>运行应用</title>
  <simpara>
   将浏览器指向已配置的域名
   （例如 <literal>http://www.example.com</literal>），响应将如下所示：
  </simpara>
  &example.outputs.similar;
  <screen>
<![CDATA[
<html>
 <head>
   <title>Hello World</title>
 </head>
 <body>
   hello world
 </body>
</html>
]]>
  </screen>
  <note>
   <simpara>
    上面的最小示例也可以使用随
    <link xlink:href="&url.git.hub;laruence/yaf/tree/master/tools/cg">Yaf
    源码仓库</link> 一起发布的 Yaf 代码生成器
    （<filename>tools/cg/yaf_cg</filename>）生成：
   </simpara>
   <screen>
<![CDATA[
$ cd tools/cg
$ ./yaf_cg -d output_directory [-a application_name] [--namespace]
]]>
   </screen>
   <simpara>
    <filename>yaf_cg</filename> 会生成比本教程所示更完整、带注释的骨架：
    一个带重写规则的 <filename>public/</filename> 入口、配置了插件和自定义路由的
    Bootstrap、示例模型、错误控制器以及视图模板。它是开发实际应用的
    一个良好起点。
   </simpara>
  </note>
 </example>

 </section>

 <section xml:id="yaf.tutorials.routes">
  <title>自定义路由</title>

 <example>
  <title>自定义路由</title>
  <simpara>
   除了默认的 <classname>Yaf_Route_Static</classname> 路由，
   还可以使用 <methodname>Yaf_Router::addRoute</methodname> 在
   <classname>Yaf_Router</classname> 路由栈上注册自定义路由，
   通常在 Bootstrap 的 <literal>_initRoute</literal> 方法中进行。
   也可以在配置文件中声明路由，并使用
   <methodname>Yaf_Router::addConfig</methodname> 加载。
  </simpara>
  <programlisting role="php">
<![CDATA[
<?php
class Bootstrap extends Yaf_Bootstrap_Abstract
{
    public function _initRoute(Yaf_Dispatcher $dispatcher)
    {
        $router = $dispatcher->getRouter();

        // /user/123 -> controller=user, action=index, id=123
        $router->addRoute("user", new Yaf_Route_Rewrite(
            "/user/:id",
            array("controller" => "user", "action" => "index")
        ));
    }
}
?>
]]>
  </programlisting>
 </example>

 </section>

 <section xml:id="yaf.tutorials.plugins">
  <title>插件</title>

 <example>
  <title>插件</title>
  <simpara>
   插件通过继承 <classname>Yaf_Plugin_Abstract</classname> 并重写其六个
   钩子方法中的一个或多个来挂入分发循环，然后通常在 Bootstrap 中使用
   <methodname>Yaf_Dispatcher::registerPlugin</methodname> 注册到分发器上：
  </simpara>
  <programlisting role="php">
<![CDATA[
<?php
class UserPlugin extends Yaf_Plugin_Abstract
{
    public function routerStartup(
        Yaf_Request_Abstract $request, Yaf_Response_Abstract $response)
    {
        // before routing: inspect or rewrite the raw request URI
    }

    public function routerShutdown(
        Yaf_Request_Abstract $request, Yaf_Response_Abstract $response)
    {
        // after routing: module/controller/action are known, good for per-route access control
    }

    public function dispatchLoopStartup(
        Yaf_Request_Abstract $request, Yaf_Response_Abstract $response)
    {
        // before the dispatch loop: one-time setup shared by all dispatches
    }

    public function preDispatch(
        Yaf_Request_Abstract $request, Yaf_Response_Abstract $response)
    {
        // before each dispatch: filter parameters or redirect before the action runs
    }

    public function postDispatch(
        Yaf_Request_Abstract $request, Yaf_Response_Abstract $response)
    {
        // after each dispatch: decorate the response or the rendered view
    }

    public function dispatchLoopShutdown(
        Yaf_Request_Abstract $request, Yaf_Response_Abstract $response)
    {
        // after the dispatch loop: final cleanup, logging or flushing
    }
}

class Bootstrap extends Yaf_Bootstrap_Abstract
{
    public function _initPlugin(Yaf_Dispatcher $dispatcher)
    {
        $dispatcher->registerPlugin(new UserPlugin());
    }
}
?>
]]>
  </programlisting>
 </example>

 </section>

</chapter>
<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"~/.phpdoc/manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
