<?xml version="1.0" encoding="gb2312"?>
<!-- $Revision: 1.1 $ -->
<!-- $Author: zhouhao007 $ -->
<!-- EN-Revision: 1.3 Maintainer: micky Status: ready -->
<section id="outcontrol.configuration">
 &reftitle.runtime;
 &extension.runtime;
 <para>
  <table>
   <title>输出控制 配置选项</title>
   <tgroup cols="3">
    <thead>
     <row>
      <entry>名称</entry>
      <entry>默认值</entry>
      <entry>作用范围</entry>
     </row>
    </thead>
    <tbody>
    <row>
     <entry>output_buffering</entry>
     <entry>"0"</entry>
     <entry>PHP_INI_PERDIR|PHP_INI_SYSTEM</entry>
    </row>
    <row>
     <entry>output_handler</entry>
     <entry>NULL</entry>
     <entry>PHP_INI_PERDIR|PHP_INI_SYSTEM</entry>
    </row>
    <row>
     <entry>implicit_flush</entry>
     <entry>"0"</entry>
     <entry>PHP_INI_PERDIR|PHP_INI_SYSTEM</entry>
    </row>
    </tbody>
   </tgroup>
  </table>
   更多关于常量 PHP_INI_* 的细节和解释参见
  <function>ini_set</function>.
 </para>
 
 &ini.descriptions.title;
 
 <para>
 <variablelist>
  <varlistentry id="ini.output-buffering">
   <term>
    <parameter>output_buffering</parameter>
    <type>boolean</type>/<type>integer</type>
   </term>
   <listitem>
    <para>
	 本标记设置为 On 的时，将在所有的脚本中使用输出控制。如果要限制输出缓冲区的最大值，可以将标记设置成该最大字节数（例如， output_buffering=4096）。
    </para>
   </listitem>
  </varlistentry>

  <varlistentry id="ini.output-handler">
   <term>
    <parameter>output_handler</parameter>
    <type>string</type>
   </term>
   <listitem>
    <para>
	 该标记可将脚本所有的输出，重定向到一个函数。例如，将 output_handler 设置为<function>mb_output_handler</function>时，
     字符的编码将被修改为指定的编码。设置的任何处理函数，将自动的处理输出缓冲。
    </para>
    <note>
     <para>
	  不能同时使用<function>mb_output_handler</function>和<function>ob_inconv_handler</function> ，
	  也不能同时使用<function>ob_gzhandler</function>和<link linkend="ini.zlib.output-compression">zlib.output_compression</link>。
     </para>
    </note>
   </listitem>
  </varlistentry>

  <varlistentry id="ini.implicit-flush">
   <term>
    <parameter>implicit_flush</parameter>
    <type>boolean</type>
   </term>
   <listitem>
    <para>
	 默认为 &false; 。如将该标记改为 &true; ，PHP将使输出层，在每段信息块输出后，自动刷新。
	 这等同于在每次使用<function>print</function>，<function>echo</function> 等函数 或每个<literal>HTML</literal>块之后，
	 调用<literal>PHP</literal>中的<function>flush</function>函数。
    </para>
    <para>
	 不在web环境中使用<literal>PHP</literal>时，打开这个选项对程序执行的性能有严重的影响，通常只推荐在调试时使用。
	 在<literal>CLI SAPI</literal>的执行模式下，该标记默认为 &true; 。
    </para>
    <para>
     参见 <function>ob_implicit_flush</function>。
    </para>
   </listitem>
  </varlistentry>
 </variablelist>
 </para>
</section>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->

