<?xml version="1.0" encoding="gb2312"?>
<!-- $Revision: 1.3 $ -->
<!-- $Author: dallas $ -->
<!-- EN-Revision:1.2 Maintainer: class007 Status: ready -->
<!-- CREDITS: cysoft -->
<sect1 id="install.openbsd">
 <title>在 OpenBSD 系统下的安装</title>
 <para>
  本章节的内容和提示仅限于将 PHP 安装到
  <ulink url="&url.openbsd;">OpenBSD 3.6</ulink> 系统上。
 </para>
 <sect2 id="install.openbsd.packages">
  <title>使用二进制包安装</title>
   <simpara>
    使用二进制包安装 PHP 到 OpenBSD 系统上是被推荐的同时也是最简单的方法。PHP
    核心包已经从其他模块中分离出来了并且每个模块可以被独立的安装/卸载而不影响其他模块。所有这些安装
    PHP 需要的文件可以在 OpenBSD 光盘或者在 FTP 站点上找到。
   </simpara>
   <simpara>
    需要安装的 PHP 核心包的文件是
    <filename>php4-core-4.3.8.tgz</filename>，它包含了基本的引擎（包括
    gettext 和 iconv）。其次，可能还需要安装一些模块包，如：<filename>php4-mysql-4.3.8.tgz</filename>
    或 <filename>php4-imap-4.3.8.tgz</filename>。需要使用命令
    <command>phpxs</command> 去激活它，并且在通过修改 &php.ini 文件来屏蔽他们。
   </simpara>
   <example id="install.openbsd.ports.example">
    <title>在 OpenBSD 系统下的软件包的安装示例</title>
    <programlisting>
<![CDATA[
# pkg_add php4-core-4.3.8.tgz
# /usr/local/sbin/phpxs -s
# cp /usr/local/share/doc/php4/php.ini-recommended /var/www/conf/php.ini
  (add in mysql)
# pkg_add php4-mysql-4.3.8.tgz
# /usr/local/sbin/phpxs -a mysql
  (add in imap)
# pkg_add php4-imap-4.3.8.tgz
# /usr/local/sbin/phpxs -a imap
  (remove mysql as a test)
# pkg_delete php4-mysql-4.3.8
# /usr/local/sbin/phpxs -r mysql
  (install the PEAR libraries)
# pkg_add php4-pear-4.3.8.tgz
]]>
    </programlisting>
   </example>
   <simpara>
   阅读<ulink url="&url.openbsd.packages;">packages(7)</ulink>
   的用户手册，可以得到更多的在 OpenBSD 系统下有关二进制软件包的信息.
   </simpara>
 </sect2>
 <sect2 id="install.openbsd.ports">
  <title>使用软件包</title>
  <simpara>
   你同样可以使用<ulink url="&url.openbsd.ports;">软件包目录（ports tree）</ulink>来编译
   PHP 的源代码。然而，这样的安装方式仅仅是建议对 OpenBSD 非常熟悉的高级用户去做。PHP4
   的软件包被分别分为了两个子目录：core 和 extensions。其中 extensions
   目录产生了所有 PHP 所支持的子模块。如果不希望创建并且使用这些模块中的某些模块，请使用
   FLAVOR <command>no_*</command> 参数。例如，如果希望跳过编译 imap
   模块，设置 FLAVOR 为 <command>no_imap</command> 即可。
  </simpara>
 </sect2>
 <sect2 id="install.openbsd.faq">
  <title>常见问题</title>
  <itemizedlist>
   <listitem>
    <simpara>
     默认安装的 Apache 运行于
     <ulink url="&url.openbsd.chroot;">chroot(2) jail</ulink>，将限制
     PHP 脚本只能访问 <filename>/var/www</filename> 下面的文件。需要建立
     <filename>/var/www/tmp</filename> 目录来存放
     PHP session 文件，或使用其它的 session 后端。此外，数据库套接字需要被放入
     jail 或者侦听 <filename>localhost</filename> 接口。如果使用网络函数，某些
     <filename>/etc</filename> 下面的文件例如
     <filename>/etc/resolv.conf</filename> 和
     <filename>/etc/services</filename> 需要被移动到
     <filename>/var/www/etc</filename> 中去。OpenBSD PEAR
     包会自动安装到正确的 chroot 目录中，因此不需要作特殊改动。有关
     OpenBSD Apache 的更多信息见
     <ulink url="&url.openbsd.apachefaq;">OpenBSD FAQ</ulink>。
    </simpara>
   </listitem>
   <listitem>
    <simpara>
     对应于 <ulink url="&url.gd;">gd</ulink> 扩展的 OpenBSD 3.6
     包需要预先安装 XFree86。如果你不想用那些需要 X11 的字体特性，则安装
     <filename>php4-gd-4.3.8-no_x11.tgz</filename> 包来替代之。
    </simpara>
   </listitem>
  </itemizedlist>
 </sect2>

 <sect2 id="install.openbsd.older">
  <title>早期发布版本</title>
  <simpara>
   早期的 OpenBSD 系统使用 FLAVORS 系统把 PHP
   链接为静态形式。自从使用这种方法编译就造成了问题即：很难制作二进制软件包。仍然可以使用早期
   stable ports trees，但这种方式已经不被 OpenBSD 小组所支持。如果对此有任何建议和意见，软件包当前的维护人是
   Anil Madhavapeddy（avsm at openbsd dot org）。
  </simpara>
 </sect2>
</sect1>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
